[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[project]
name = "ssm-configs"
version = "0.1.0"
description = "Configuration schemas and registries for sequential sampling models"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12,<3.15"
dependencies = [
    "pluggy>=1.5.0",
    "pydantic>=2.13.4",
]
